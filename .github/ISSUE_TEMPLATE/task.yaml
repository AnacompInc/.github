name: "🛠 Task"
description: "General work item (infra, cleanup, refactor, etc.)"
labels: ["type: task"]
body:
  - type: markdown
    attributes:
      value: |
        > **Internal use.** Do not include secrets, credentials, or PII in issue text or attachments.

  - type: textarea
    id: details
    attributes:
      label: "Details"
      placeholder: "What needs to be done?"
    validations:
      required: true

  - type: textarea
    id: outcome
    attributes:
      label: "Expected Outcome"
      placeholder: "What should success look like?"

  - type: dropdown
    id: priority
    attributes:
      label: "Priority (optional)"
      options:
        - "Unassigned"
        - "P0 – Critical"
        - "P1 – High"
        - "P2 – Medium"
        - "P3 – Low"
      default: 0

  - type: checkboxes
    id: pii_check
    attributes:
      label: Data Safety Verification
      description: Confirm that no PII, PHI, credentials, or other sensitive data is included in this issue.
      options:
        - label: I confirm this issue does not contain sensitive data.
          required: true
